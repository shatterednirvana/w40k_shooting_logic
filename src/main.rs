fn main() {
    /*
     * ok, here's the basic idea:
     *
     * loop {
     *   generate strength (random number between 2 and 12)
     *   generate toughness (random number between 2 and 12)
     *   figure out what the correct min dice roll would be from this
     *   ask the user what they think
     *   compare them
     * }
     *
     */

    println!("Hello, world!");
}
